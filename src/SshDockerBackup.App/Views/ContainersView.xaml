<UserControl x:Class="SshDockerBackup.App.Views.ContainersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:SshDockerBackup.App.Converters">

    <UserControl.Resources>
        <conv:StateToBrushConverter x:Key="StateToBrush" />
        <conv:InverseBooleanConverter x:Key="InverseBool" />
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Containers" Style="{StaticResource HeadingText}" />
            <TextBlock Style="{StaticResource SubtleText}"
                       Text="Tick the containers to back up. What matters is the mount data: named volumes live under the Docker root directory, which is not a shared folder and so is invisible to Hyper Backup." />
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Refresh" MinWidth="90" Padding="12,6"
                        Command="{Binding RefreshCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
                <Separator Margin="10,0" />
                <Button Content="All" MinWidth="60" Margin="0,0,6,0" Command="{Binding SelectAllCommand}" />
                <Button Content="None" MinWidth="60" Margin="0,0,6,0" Command="{Binding SelectNoneCommand}" />
                <Button Content="Only with data" MinWidth="110" Margin="0,0,6,0" Command="{Binding SelectWithDataCommand}" />
                <Separator Margin="10,0" />
                <Button Content="Calculate sizes" MinWidth="120"
                        Command="{Binding CalculateSizesCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
                <ProgressBar Width="90" Height="4" IsIndeterminate="True" Margin="12,0,0,0" VerticalAlignment="Center"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
            </StackPanel>

            <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" Opacity="0.7"
                       HorizontalAlignment="Right" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" Margin="12,0,0,0" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding State.Containers}"
                  SelectedItem="{Binding SelectedContainer}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <!-- Template column rather than DataGridCheckBoxColumn: the built-in one needs the
                     cell to enter edit mode first, so the first click never registers. -->
                <DataGridTemplateColumn Header="" Width="44" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Name" Width="220" IsReadOnly="True" SortMemberPath="Name">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,8,0"
                                         Fill="{Binding State, Converter={StaticResource StateToBrush}}" />
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Image" Width="*" Binding="{Binding Image}" IsReadOnly="True" />
                <DataGridTextColumn Header="Status" Width="150" Binding="{Binding Status}" IsReadOnly="True" />
                <DataGridTextColumn Header="Data" Width="160" Binding="{Binding MountSummary}" IsReadOnly="True" />
                <DataGridTextColumn Header="Size" Width="90" Binding="{Binding SizeText}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Detail pane -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock FontWeight="SemiBold" Margin="0,0,0,6"
                               Text="{Binding SelectedContainer.Name, FallbackValue='Select a row to see its mounts', TargetNullValue='Select a row to see its mounts'}" />
                    <TextBlock Style="{StaticResource MonoText}" TextWrapping="Wrap"
                               Text="{Binding SelectedContainer.MountDetail}" />
                    <TextBlock Style="{StaticResource SubtleText}" Margin="0,6,0,0"
                               Text="{Binding SelectedContainer.ComposeProjectText}" />
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                    <TextBlock Opacity="0.7" HorizontalAlignment="Right">
                        <Run Text="{Binding SelectedCount, Mode=OneWay}" />
                        <Run Text="selected" />
                    </TextBlock>
                    <TextBlock Opacity="0.7" HorizontalAlignment="Right" Margin="0,4,0,0"
                               Visibility="{Binding HasMeasuredSizes, Converter={StaticResource BoolToVis}}">
                        <Run Text="{Binding SelectedSizeText, Mode=OneWay}" />
                        <Run Text="of" />
                        <Run Text="{Binding TotalSizeText, Mode=OneWay}" />
                        <Run Text="total" />
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
